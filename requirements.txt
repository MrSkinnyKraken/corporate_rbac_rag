# =============================================================================
# Production dependencies (as for now).
# Pinned to versions validated during the Phase 1-3 notebook experiments.
# =============================================================================

# --- Web framework (FastAPI backend) -----------------------------------------
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9         # multipart/form-data parsing for file uploads

# --- Web UI (Streamlit frontend) ---------------------------------------------
streamlit>=1.40.0,<2.0.0
httpx==0.28.1                    # frontend → backend HTTP client

# --- Configuration management ------------------------------------------------
pydantic==2.12.5
pydantic-settings>=2.5.2,<3.0.0
python-dotenv==1.0.1

# --- Vector store + lexical retrieval ---------------------------------------
chromadb==0.4.24                 # used as HttpClient against a local service
rank-bm25==0.2.2
numpy==1.26.4
sentence-transformers==5.2.3     # pulls torch + transformers transitively

# --- LangChain primitives (Document type, embedding adapters, loaders) ------
langchain-core==0.1.53
langchain-community==0.0.29
langchain-huggingface==0.0.3

# --- Document loaders (raw → text) -------------------------------------------
pypdf==4.1.0                     # .pdf
python-docx==1.2.0               # .docx (Word)
openpyxl==3.1.5                  # .xlsx (Excel)
python-pptx==1.0.2               # .pptx (PowerPoint)
beautifulsoup4==4.14.3           # .html / .htm

# --- Local LLM (Ollama HTTP client) ------------------------------------------
requests==2.32.5
